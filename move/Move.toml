[package]
name = "CoffeeShop"
version = "0.0.1"

[addresses]
coffee_shop = "_" # The underscore will allow the CLI to replace it with your address

[dependencies]
AptosFramework = { git = "https://github.com/aptos-labs/aptos-framework.git", subdir = "aptos-framework", rev = "mainnet" }